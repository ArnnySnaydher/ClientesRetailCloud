version: '3.8'

services:
  techretail-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: techretail-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped

  sonarqube:
    image: sonarqube:9.9-community
    container_name: sonarqube-techretail
    ports:
      - "9000:9000"
    environment:
      - SONAR_FORCEAUTHENTICATION=false
    restart: unless-stopped